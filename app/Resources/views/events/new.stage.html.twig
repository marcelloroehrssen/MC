{% extends 'base.html.twig' %}

{% block body %}
    <h1>Stage creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Add Stage" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('events_slot_new', {'id':event.id}) }}">Go To Step 3</a>
        </li>
    </ul>
{% endblock %}
